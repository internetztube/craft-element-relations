{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    first: true,
    label: 'Bulk-refresh batch size',
    name: 'bulkRefreshBatchSize',
    value: settings.bulkRefreshBatchSize,
    instructions: 'Specify the maximum number of elements to handle per queue job during a bulk relation refresh.' | t('expanded-singles'),
}) }}
